# Система прав пользователей Maporia

## Обзор

В Maporia реализована система ролевого доступа с четырьмя типами пользователей. Права определяются на основе полей в таблице `profiles` и проверяются как на уровне базы данных (RLS policies), так и на уровне приложения.

---

## Типы пользователей

### 1. **Guest (Гость)** 👤
**Описание**: Неавторизованный пользователь

**Права:**
- ✅ Просмотр публичных мест
- ✅ Просмотр карты, списков и страниц публичных мест
- ✅ Просмотр публичных профилей

**Ограничения:**
- ❌ Не может лайкать, комментировать или сохранять места
- ❌ Не может добавлять места
- ❌ Не может просматривать премиум-места
- ❌ Не может редактировать профиль

**UX**: При попытке выполнить ограниченное действие открывается модальное окно авторизации (регистрация / вход)

**Определение в коде:**
```typescript
// app/lib/access.ts
if (!profile) {
  return {
    role: "guest",
    hasPremium: false,
    isAdmin: false,
  };
}
```

---

### 2. **Standard User (Стандартный пользователь)** 👥
**Описание**: Авторизованный пользователь без активной подписки

**Права:**
- ✅ Все права Guest
- ✅ Лайкать места
- ✅ Комментировать места
- ✅ Сохранять места в избранное
- ✅ Просматривать свой профиль
- ✅ Редактировать свой профиль

**Ограничения:**
- ❌ Не может создавать места (только Premium и Admin)
- ❌ Не может просматривать премиум-места
- ❌ Не может добавлять или редактировать премиум-места
- ❌ Не может изменять `access_level` места на `premium`

**UX**: Премиум-контент визуально заблокирован с призывом к обновлению до Premium

**Определение в коде:**
```typescript
// app/lib/access.ts
// Стандартный пользователь - авторизован, но subscription_status != 'active'
return {
  role: "standard",
  hasPremium: false,
  isAdmin: false,
  subscriptionStatus: "inactive",
};
```

**Поля в БД:**
- `role = 'standard'`
- `subscription_status = 'inactive'` (или NULL)
- `is_admin = FALSE`

---

### 3. **Premium User (Премиум пользователь)** ⭐
**Описание**: Авторизованный пользователь с активной платной подпиской

**Условие**: `subscription_status = 'active'`

**Права:**
- ✅ Все права Standard User
- ✅ Просмотр премиум-мест
- ✅ Доступ к премиум-коллекциям и маршрутам
- ✅ Сохранение премиум-мест
- ✅ Создание премиум-мест
- ✅ Изменение `access_level` места на `premium`

**Ограничения:**
- ❌ Нет административных привилегий

**Определение в коде:**
```typescript
// app/lib/access.ts
if (subscriptionStatus === "active") {
  return {
    role: "premium",
    hasPremium: true,
    isAdmin: false,
    subscriptionStatus: "active",
  };
}
```

**Поля в БД:**
- `role = 'premium'`
- `subscription_status = 'active'`
- `is_admin = FALSE`

---

### 4. **Admin (Администратор)** 👑
**Описание**: Администратор платформы

**Условие**: `is_admin = TRUE` ИЛИ `role = 'admin'`

**Права:**
- ✅ Полный доступ ко всему контенту
- ✅ Просмотр, создание, редактирование, удаление любых мест
- ✅ Изменение `access_level` любых мест (public/premium)
- ✅ Модерация комментариев и пользователей
- ✅ Доступ к админ-интерфейсу и инструментам
- ✅ Редактирование профилей других пользователей
- ✅ Изменение ролей и прав пользователей
- ✅ Автоматически имеет премиум-доступ

**Ограничения:**
- ❌ Нет ограничений

**Определение в коде:**
```typescript
// app/lib/access.ts
if (profile.is_admin === true) {
  return {
    role: "admin",
    hasPremium: true, // Admin автоматически имеет премиум-доступ
    isAdmin: true,
    subscriptionStatus: profile.subscription_status || undefined,
  };
}
```

**Поля в БД:**
- `role = 'admin'`
- `is_admin = TRUE`
- `subscription_status` может быть любым (не влияет на права)

---

## Структура базы данных

### Таблица `profiles`

| Поле | Тип | Описание | Значения по умолчанию |
|------|-----|----------|----------------------|
| `role` | TEXT | Роль пользователя | `'standard'` |
| `subscription_status` | TEXT | Статус подписки | `'inactive'` |
| `is_admin` | BOOLEAN | Является ли администратором | `FALSE` |

**Ограничения:**
- `role` может быть: `'guest'`, `'standard'`, `'premium'`, `'admin'`
- `subscription_status` может быть: `'active'`, `'inactive'`

**Важно:**
- `guest` не хранится в БД - определяется отсутствием сессии
- Роль определяется динамически на основе `is_admin` и `subscription_status`
- Администраторы автоматически имеют премиум-доступ

---

## Логика определения роли

Функция `getUserAccess()` в `app/lib/access.ts` определяет роль пользователя в следующем порядке:

1. **Guest**: Нет профиля или нет авторизованного пользователя
2. **Admin**: `profile.is_admin === true` ИЛИ `profile.role === 'admin'`
3. **Premium**: `profile.subscription_status === 'active'` (и не админ)
4. **Standard**: Авторизован, но нет активной подписки

```typescript
export function getUserAccess(profile: Profile | null): UserAccess {
  // 1. Guest
  if (!profile) {
    return { role: "guest", hasPremium: false, isAdmin: false };
  }

  // 2. Admin (проверяется первым)
  if (profile.is_admin === true) {
    return {
      role: "admin",
      hasPremium: true,
      isAdmin: true,
      subscriptionStatus: profile.subscription_status || undefined,
    };
  }

  // 3. Premium
  if (profile.subscription_status === "active") {
    return {
      role: "premium",
      hasPremium: true,
      isAdmin: false,
      subscriptionStatus: "active",
    };
  }

  // 4. Standard
  return {
    role: "standard",
    hasPremium: false,
    isAdmin: false,
    subscriptionStatus: profile.subscription_status || "inactive",
  };
}
```

---

## Проверка прав доступа

### Основные функции проверки

Все функции находятся в `app/lib/access.ts`:

#### 1. `canUserViewPlace(userAccess, place)`
Проверяет, может ли пользователь просмотреть место.

```typescript
// Публичные места - доступны всем
// Премиум-места - только для premium/admin
return userAccess.hasPremium === true || !isPlacePremium(place);
```

#### 2. `canUserCreatePremiumPlace(userAccess)`
Проверяет, может ли пользователь создавать премиум-места.

```typescript
// Только premium и admin
return userAccess.role === "premium" || userAccess.role === "admin";
```

#### 3. `canUserInteract(userAccess)`
Проверяет, может ли пользователь лайкать/комментировать/сохранять.

```typescript
// Только авторизованные пользователи
return userAccess.role !== "guest";
```

#### 4. `canUserAddPlace(userAccess)`
Проверяет, может ли пользователь добавлять места.

```typescript
// Только premium пользователи и админы
return userAccess.role === "premium" || userAccess.role === "admin";
```

#### 5. `isUserAdmin(userAccess)`
Проверяет, является ли пользователь администратором.

```typescript
return userAccess.role === "admin";
```

---

## RLS Policies (Row Level Security)

Политики безопасности на уровне строк в Supabase обеспечивают контроль доступа на уровне базы данных.

### Таблица `places`

#### SELECT (Просмотр)
- ✅ **Публичные места**: Доступны всем (включая гостей)
- ✅ **Премиум-места**: Только для premium пользователей, админов и владельцев
- ✅ **Свои места**: Пользователь всегда может просмотреть свои места

#### INSERT (Создание)
- ✅ **Публичные места**: Только premium пользователи и админы
- ✅ **Премиум-места**: Только premium пользователи и админы

#### UPDATE (Редактирование)
- ✅ **Свои места**: Пользователь может редактировать свои места
- ❌ **Стандартные пользователи**: Не могут изменить `access_level` на `premium`
- ✅ **Админы**: Могут редактировать любые места

#### DELETE (Удаление)
- ✅ **Свои места**: Пользователь может удалить свои места
- ✅ **Админы**: Могут удалить любое место

### Таблица `reactions` (лайки/избранное)

- ✅ **Создание**: Только авторизованные пользователи
- ✅ **Просмотр**: Только свои реакции
- ✅ **Удаление**: Только свои реакции

### Таблица `comments` (комментарии)

- ✅ **Создание**: Только авторизованные пользователи (если `comments_enabled = TRUE`)
- ✅ **Просмотр**: 
  - Комментарии к публичным местам - все
  - Комментарии к премиум-местам - только premium пользователи и админы
- ✅ **Редактирование/Удаление**: Только свои комментарии
- ✅ **Удаление**: Админы могут удалить любой комментарий

### Таблица `profiles`

- ✅ **Просмотр**: Все могут просматривать профили (для публичных страниц)
- ✅ **Редактирование**: 
  - Пользователь может редактировать свой профиль
  - ❌ НЕ может изменить `role` или `is_admin`
  - ✅ Админы могут редактировать любые профили (включая `role` и `is_admin`)

---

## Использование в компонентах

### Hook `useUserAccess()`

```typescript
import { useUserAccess } from '@/hooks/useUserAccess';

function MyComponent() {
  const { loading, user, profile, access } = useUserAccess();
  
  // access содержит:
  // {
  //   role: "guest" | "standard" | "premium" | "admin",
  //   hasPremium: boolean,
  //   isAdmin: boolean,
  //   subscriptionStatus?: "active" | "inactive"
  // }
}
```

### Проверка прав в компонентах

```typescript
import { useUserAccess } from '@/hooks/useUserAccess';
import { canUserInteract, canUserViewPlace, isUserAdmin } from '@/lib/access';

function PlaceComponent({ place }) {
  const { access, loading } = useUserAccess();
  
  if (loading) return <Loading />;
  
  // Проверка возможности взаимодействия
  if (!canUserInteract(access)) {
    return <AuthModal isOpen={true} />;
  }
  
  // Проверка возможности просмотра премиум-места
  if (!canUserViewPlace(access, place)) {
    return <LockedPlaceOverlay placeTitle={place.title} />;
  }
  
  // Проверка админ-прав
  if (isUserAdmin(access)) {
    return <AdminControls place={place} />;
  }
}
```

### Защита маршрутов

Маршруты автоматически защищены через `middleware.ts`:
- `/profile` - требует авторизации
- `/saved` - требует авторизации
- `/add` - требует авторизации
- `/places/*/edit/*` - требует авторизации

Дополнительная защита в компонентах:

```typescript
export default function ProtectedPage() {
  const { user, loading } = useUserAccess(true); // requireAuth = true
  
  if (loading) return <Loading />;
  if (!user) return null; // Перенаправит на /auth
  
  return <PageContent />;
}
```

---

## Примеры использования

### Пример 1: Редактор доступа к месту

```typescript
// app/places/[id]/edit/access/page.tsx
const { access } = useUserAccess(true);
const canCreatePremium = canUserCreatePremiumPlace(access);

// Если пользователь не может создавать премиум-места,
// опция "premium" будет заблокирована
```

### Пример 2: Проверка премиум-доступа

```typescript
// app/hooks/usePremiumGate.ts
const canAccessPlace = (place: Place) => {
  if (isPremium) return true;
  const placeIsPremium = isPlacePremium(place);
  if (!placeIsPremium) return true;
  return canUserViewPlace(access, place);
};
```

### Пример 3: Админ-панель

```typescript
const { access } = useUserAccess();

if (!isUserAdmin(access)) {
  router.push('/');
  return null;
}

// Показать админ-интерфейс
```

---

## Миграция и настройка

### SQL скрипты

1. **`add-user-roles-fields.sql`** - Добавляет поля `role`, `subscription_status`, `is_admin`
2. **`rls-role-based-policies.sql`** - Настраивает RLS политики

### Установка админа

```sql
-- Установить пользователя как админа
UPDATE profiles 
SET is_admin = TRUE, role = 'admin' 
WHERE id = 'user-id-here';
```

### Установка премиум-статуса

```sql
-- Установить премиум-статус
UPDATE profiles 
SET subscription_status = 'active', role = 'premium' 
WHERE id = 'user-id-here';
```

---

## Важные замечания

1. **Guest пользователи** не хранятся в БД - они определяются отсутствием сессии
2. **Роль определяется динамически** на основе `is_admin` и `subscription_status`
3. **Админы автоматически имеют премиум-доступ** (`hasPremium = true`)
4. **Все проверки прав** должны использовать функции из `app/lib/access.ts` для консистентности
5. **RLS политики** обеспечивают безопасность на уровне БД, даже если фронтенд проверки обойдены
6. **Пользователи не могут изменять** свои `role` или `is_admin` - только админы могут

---

## Файлы системы прав

### Основные файлы

- **`app/lib/access.ts`** - Утилиты для проверки прав и определения ролей
- **`app/hooks/useUserAccess.ts`** - React hook для получения прав пользователя
- **`app/hooks/usePremiumGate.ts`** - Hook для проверки премиум-доступа
- **`middleware.ts`** - Защита маршрутов Next.js
- **`rls-role-based-policies.sql`** - RLS политики Supabase
- **`add-user-roles-fields.sql`** - Миграция полей ролей

### Документация

- **`README-ROLES-IMPLEMENTATION.md`** - Полная документация по реализации (на английском)

---

## Тестирование прав

### Тест Guest
1. Выйти из аккаунта
2. Проверить:
   - ✅ Можно просматривать публичные места
   - ❌ Нельзя лайкать/комментировать
   - ❌ При попытке взаимодействия открывается модальное окно авторизации

### Тест Standard User
1. Создать аккаунт (без подписки)
2. Проверить:
   - ✅ Можно лайкать/комментировать публичные места
   - ❌ Нельзя просматривать премиум-места
   - ❌ Нельзя создавать премиум-места
   - ✅ Показывается CTA для обновления до Premium

### Тест Premium User
1. Установить `subscription_status = 'active'`
2. Проверить:
   - ✅ Можно просматривать премиум-места
   - ✅ Можно создавать премиум-места
   - ✅ Полный доступ ко всем функциям

### Тест Admin
1. Установить `is_admin = TRUE`
2. Проверить:
   - ✅ Можно просматривать/редактировать/удалять любые места
   - ✅ Можно изменять `access_level` любых мест
   - ✅ Можно редактировать профили других пользователей
   - ✅ Полный доступ ко всем функциям

---

## Сводная таблица прав

| Действие | Guest | Standard | Premium | Admin |
|----------|-------|----------|---------|-------|
| Просмотр публичных мест | ✅ | ✅ | ✅ | ✅ |
| Просмотр премиум-мест | ❌ | ❌ | ✅ | ✅ |
| Лайкать места | ❌ | ✅ | ✅ | ✅ |
| Комментировать | ❌ | ✅ | ✅ | ✅ |
| Сохранять в избранное | ❌ | ✅ | ✅ | ✅ |
| Добавлять публичные места | ❌ | ❌ | ✅ | ✅ |
| Добавлять премиум-места | ❌ | ❌ | ✅ | ✅ |
| Редактировать свои места | ❌ | ✅ | ✅ | ✅ |
| Редактировать чужие места | ❌ | ❌ | ❌ | ✅ |
| Удалять свои места | ❌ | ✅ | ✅ | ✅ |
| Удалять чужие места | ❌ | ❌ | ❌ | ✅ |
| Изменять access_level на premium | ❌ | ❌ | ✅ | ✅ |
| Редактировать свой профиль | ❌ | ✅ | ✅ | ✅ |
| Редактировать чужие профили | ❌ | ❌ | ❌ | ✅ |
| Изменять роли пользователей | ❌ | ❌ | ❌ | ✅ |
